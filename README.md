I am not explaining the entire functionality here, but only typing to make an important comment. Since Github can only display static web pages, whenever one adds a new recipe and clicks the add button, they must refresh after doing so in order to notice the change, and the change will be permanent - meaning even after reopening the link it will be there, but because of GitHub's static nature due to which files cannot be automatically updatd, the makeshift database that I have made by the name of recipes.JSON does not show the new recipe stored in the database unless I committ it, as it would only be stored locally in my computer. The recipes.JSON file only shows the recipes that were added in that file up untill the point it got added in this repository, nevertheless the web page works as expected, as long as you make sure and remember to click the "add new recipe" button once and then refresh the page. 
